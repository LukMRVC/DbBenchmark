// Parameters:
// - Parameter 1: Table object in the class Database
// - Parameter 2: Method name
// - Parameter 3: The number of parameters followed by data types of parameters
// - Last parameter: The number of queries send to the database
//
// operace 3.2.2 SELECT 1 FROM participant WHERE contract_id=$contract_id AND deleted_at IS NULL and participant_id <> $participant_id
//
// Participant SelectOther
//
// operace 3.2.5 UPDATE voip_number SET participant_id=NULL, current_state=0, activated=NULL, quarantine_until= (current_timestamp + INTERVAL ‘6 MONTH‘) WHERE participant_id=$participant_id
//
// operace 3.4 SELECT * from participant where contract_id
// TODO: Uncomment
//Participant SelectForContract 1 Contract 500
// operace 4.5 UPDATE voip_number set participant_id=NULL, current_state=0, activated_at=NULL, quarantine_until=current_timestamp +  INTERVAL ‘6 MONTHS‘ where participant_id=…
// TODO: Uncomment
//VoipNumber NumberRemoval 1 VoipNumber 300
// operace 5.1.6 INSERT INTO call_detail_record(disposition, source_num, destination_num, length, call_date, number_id, incoming_outgoing, price_list_id) VALUES($disposition, $source_num, $dest_num, $length, $call_date, $number_id, $incoming_outgoing, $price_list_id)
//
// operace 5.3 SELECT * from call_detail_record where number_id IN (select number_id from voip_number where participant_id=…)
CallDetailRecord SelectForParticipant 1 Participant 500
// operace 7.3 SELECT * from invoice
// TODO: Uncomment
// Invoice Select 0 500
// operace 9.4.2 UPDATE voip_number SET participant_id=(SELECT participant_id FROM number_request WHERE number_id=$number_id), current_state=1, activated=current_timestamp WHERE number_id=$number_id
//
// operace 10.2.1 SELECT contract_id FROM contract WHERE deleted_at is NULL OR (date_part(‘year’, current_timestamp) – date_part(‘year’, contract.deleted_at) * 12 + date_part(‘month’, current_timestamp – (current_timestamp - INTERVAL ‘1 DAY’))) = 0
//
// operace 10.4.1 SELECT length, tariffication_first, tariffication_second, price_per_second FROM call_detail_record cdr JOIN price_list ON cdr.price_list_id=price_list.price_list_id WHERE EXISTS(SELECT number_id FROM voip_number WHERE participant_id=$participant_id AND cdr.number_id=voip_number.number_id) AND length > 0 AND call_date >= period_start AND call_date <= period_end